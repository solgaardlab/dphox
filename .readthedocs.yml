version: 2

conda:
  environment: .conda.yml

python:
  version: 3.9
  install:
    - requirements: doc/requirements.txt
    - method: setuptools
      path: .

sphinx:
  builder: html
  configuration: doc/source/conf.py