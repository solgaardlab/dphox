version: 2

conda:
  environment: .conda.yml

python:
  version: 3.8
  install:
    - requirements: doc/optional_requirements.txt
    - method: setuptools
      path: .

sphinx:
  builder: html
  configuration: doc/source/conf.py